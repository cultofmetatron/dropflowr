Application.CollectionView.extend({
  name: "disco/playlist",
  collection: Application.Collections['disco/playlist']
});

// Instances of this view can be created by calling:
// new Application.Views["disco/playlist"]()
